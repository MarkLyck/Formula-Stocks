import React from 'react'
import withDashboard from '~/components/Dashboard/withDashboard'

const RoadMap = () => {
  return <div>Road map (coming soon)</div>
}

export default withDashboard(RoadMap)
