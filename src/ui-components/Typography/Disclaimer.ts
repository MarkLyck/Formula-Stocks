import styled from '@emotion/styled'

export const Disclaimer = styled.p`
  color: ${(props: any) => props.theme.palette.basic[900]};
  font-weight: 100;
  font-size: 0.8rem;
  line-height: 1;
  margin: 8px 0;
`
